---
description: Resume TodoWrite markdown mirroring for this project (removes .paused flag)
allowed-tools: Bash(bash:*)
---

Status: !`bash ${CLAUDE_PLUGIN_ROOT}/scripts/resume.sh`

Mirroring resumed for this project. The next `TodoWrite` call will refresh `.claude/todos/session-*.md` again, and any `/todos-watch` Terminal monitor will start showing live updates.
